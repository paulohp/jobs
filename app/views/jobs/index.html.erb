<div class="section-head">
  <div class="container">
    <h1>Ultimos envios</h1>
  </div>
  <!-- /.container --> 
</div>
<!-- /.section-head -->
<div class="container inner">
  <div class="row">
  <div class="span8">
    <% @jobs.each do |job| %>
        <%= job.name_and_category %>
        <%= link_to job do %>
            <h1><%=job.title%></h1>
        <% end %>
        <div>
            <%= job.created_at.to_s(:post) %>
        </div>
        <div>
            <%= job.description.html_safe %>
        </div>
        
        <%= link_to "Ver mais", job %>
      
    <% end %>
  </div>
  <div class="span4">
      Outro Lado
  </div>
</div>
<!-- /.container -->
